import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#fff0ea',
    },
    secondary: {
      main: '#370202',
    },
  },

  typography: {
    fontFamily: 'Roboto',
    h5: {
      fontFamily: 'Pacifico',
    },
    h4: {
      fontFamily: 'Pacifico',
    },
  },
});
