import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#f0e9e5',
    },
    secondary: {
      main: '#370202',
    },
  },

  typography: {
    fontFamily: 'Roboto',
    h5: {
      fontFamily: 'Pacifico',
    },
    h4: {
      fontFamily: 'Pacifico',
    },
  },
});
