import { createTheme } from '@mui/material';

export const orange = createTheme({
  palette: {
    background: {
      beige: '#fff0ea',
    },
    orange: {
      main: '#ffb99e',
    },
    primary: {
      main: '#fff0ea',
    },
    secondary: {
      main: '#4c031f',
    },
  },
});
