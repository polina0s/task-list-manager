import { createTheme } from '@mui/material';

export const orange = createTheme({
  palette: {
    orange: {
      main: '#ffb99e',
      light: '#ffba9eb6',
      dark: '#f69873b6',
      contrastText: '#242105',
    },
  },
});
